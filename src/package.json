{
  "name": "changed-files",
  "version": "0.0.0",
  "description": "Set variables based on changed files in the commit",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc && .\\node_modules\\.bin\\browserify ./detectchangedfilestask/task.js -o ./detectchangedfilestask/index.js",
    "clean": "del .\\detectchangedfilestask\\*.js",
    "pack": "tfx extension create --manifestglobs vss-extension.json"
  },
  "author": "Mathijs van Gool",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.18.0",
    "vsts-task-lib": "^2.3.0"
  },
  "devDependencies": {
    "@types/axios": "^0.14.0",
    "@types/node": "^9.4.6",
    "@types/q": "^1.0.7",
    "browserify": "^16.1.1"
  }
}
